en:
  activerecord:
    attributes:
      review:
        reviewable: "Reviewable entity with specified type and id"
    errors:
      messages:
        blank: "is required"
        required: "was not found in database"
